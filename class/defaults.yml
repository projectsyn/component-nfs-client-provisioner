parameters:
  nfs_client_provisioner:
    multi_instance: true
    namespace: syn-nfs-client-provisioner
    server: nfs.local
    path: /srv/nfs/export
    pvMountOptions: null
    provisionerName: cluster.local/nfs-client-provisioner-${_instance}
    storageClasses:
      nfs: {}
    charts:
      nfs_client_provisioner: 1.2.9
